# Female bald-base replacement slot

Replace the existing runtime PNGs in this folder with the new bald female base, keeping these exact filenames:

- `idle.png` — 384×384, 4×4 cells, 96×96 each
- `walk.png` — 768×384, 8×4 cells, 96×96 each
- `run.png` — 768×384, 8×4 cells, 96×96 each

Rows must be: `down`, `left`, `right`, `up`.
Ground anchor must remain `(48, 84)`.
Keep `sprite.json` unchanged for this first test.

No separate hair or eye assets are required yet. First prove the bald female and bald male body sheets load and animate correctly.
